export declare class Renderer {
    constructor();
    drawCircle(context: any, x: number, y: number, radius: number, color: string): void;
    drawOrbit(context: any, x: number, y: number, radius: number, color: string): void;
}
